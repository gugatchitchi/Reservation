# TODO: Edit name
name: code
# TODO: Edit description
description: A new Flutter project.

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  material_design_icons_flutter: ^4.0.5755
  provider: ^4.3.2+2

  # For Connectiong to Back End
  firebase_core: ^0.5.0+1
  cloud_firestore: ^0.14.1+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
  #   - images/a_dot_ham.jpeg

  fonts:
    - family: Georgian
      fonts:
        - asset: assets/fonts/bpg_nino_mtavruli_normal.ttf
        - asset: assets/fonts/bpg_nino_mtavruli_bold.ttf
          weight: 800
