import 'package:flutter/material.dart';

// ============================
// this class contains all the main
// colors used in the application
// ============================
class Palette {
  // background color
  static Color bg = Color.fromRGBO(250, 250, 250, 1);

  // Accent Color
  static Color accent = Color.fromRGBO(252, 156, 120, 1);

  // Font Colors
  static Color fontDark = Color.fromRGBO(115, 115, 117, 1);
  static Color fontGrey = Color.fromRGBO(190, 190, 190, 1);
  static Color fontLight = Color.fromRGBO(250, 250, 250, 1);
}
